# 2026-09-15 レビュー対応・ローカル検証記録

この文書の日付は作業日です。本番公開日・医院方針の改定日ではありません。

## 作業範囲

- 開始時: `codex/calendar-next-day-guidance`、`aab6067789568ab41adc92515f3a743674209d05`。作業ツリーはクリーン。開始時の `npm run build` は成功（必須24ファイル・11 HTML）。
- 現行コードで指摘を再確認し、既存ブランチの未コミット差分として修正。今回の指示を優先し、commit・push・PR作成・merge・デプロイ・ワークフロー手動実行・配信設定変更は行っていません。
- 旧Preview `009a8a76` は今回の編集を含みません。新しいPreview URLは作成していません。
- npm依存・外部サービス・テストフレームワークの追加なし。電話発信・予約・問い合わせ送信なし。

## 修正と確認方法

| 対象ファイル | 修正前の問題と修正後の動作 | 確認 |
|---|---|---|
| `JS/clinic-hours.js`, `JS/holidays-data.js`, `contact.html` | データ欠落・不正・対象年外を通常診療とみなせたため、状況・医師・カレンダー・次回案内を「確認できません」に統一。問い合わせページのデータ読込不足も補完。空の正常データは区別。収録年の12月31日まで有効。掲載期限後も休診・担当変更を適用。同じ受付状態の再描画を抑制 | 本体JSの境界・異常系テスト、ブラウザー |
| `scripts/update-holidays.mjs`, `.github/workflows/update-holidays.yml` | HTTP/HTML/実在日/重複/件数/年範囲/既存日の欠落を検証。一時ファイルから置換し、失敗時は既存保持、同じ内容なら無変更。未公表の翌年を必須にしない。既存自動反映処理の前にテスト・buildを追加 | 本体import＋通信モック・一時出力試験。既存1067日を維持し、対象年メタデータだけ追加 |
| `JS/date-utils.js`, `JS/news-loader.js`, `JS/news-popup.js`, `index.html`, `news.html`, `gastroscopy.html`, `lifestyle-disease.html` | 日本時間で掲載期間を共通判定。popupUntilも同時に満たす。不正日付・逆転期間を非掲載。表示直前・深夜・タブ/ページ復帰で更新。継続記事DOMと閉じたpopupの状態を保持 | 本体テスト、4タイムゾーン、ブラウザー時計操作 |
| `404.html`, `scripts/dev.mjs`, `JS/script.js` | 深いURLで壊れる相対参照をルート相対へ。動的ロゴも修正。ローカルサーバーが404 HTMLをHTTP 404で返す | 3種の存在しないURLで応答・リソース・復帰を確認。Cloudflare上は未検証 |
| `JS/site-config.js`, `JS/analytics-consent.js` | HTTPS＋確認済み本番ホスト名の完全一致だけGA初期化。測定ID、広告無効、URLクエリ/フラグメント除外を保持 | 本体JSをVMで実行、ネットワークなし。9種の非本番URLで初期化0、本番モックのみ1回 |
| `index.html` | availableServiceのMedicalConditionを、既存本文にある食事・運動療法のMedicalTherapyへ修正 | JSON構文とSchema.orgの型の意味を別々に確認 |
| `gastroscopy.html`, `service.html` | 市の検診の年齢・住民票・職場での受診機会・年度内1回の条件を掲載。追加依頼により、受診方法は症状や検査・治療の状況をうかがって案内する表現へ統一し、公式案内へのリンクを維持。関連FAQ/JSON-LDにも整合。2027年1月以降の既存料金合算表を追加し、計算に使用した市の基本料金は令和8年度の金額と注記 | 佐世保市一次資料、本文/JSON-LD/算術のテスト |
| `privacy.html`, `lifestyle-disease.html` | 医療連携と法定例外を分離し、黙示同意の条件を明記。低コレステロールをHDL（善玉）の低値へ明確化 | 個人情報保護委員会、厚生労働省資料 |
| `gastroscopy.html`, `doctors.html`, `access.html`, `index.html`, `lifestyle-disease.html`, `privacy.html`, `JS/script.js`, `JS/transition-init.js`, `CSS/style.css` | 胃カメラの6項目目次、FAQ回答を開いて移動、小田医師の第4土曜案内、短い受付表示から例外を含む表へリンク。受付時間/カレンダー/アクセス移動の420ms＋620ms待機を省略。共通JS失敗時の本文復旧、動きを減らす設定で滑らかなスクロールを停止。メニュー/駐車場拡大の表示開始時のvisibility切替を即時にし、開いた直後のフォーカス失敗を解消 | 操作・表示・取得失敗のブラウザー確認 |
| `scripts/build.mjs`, `package.json`, `tests/` | 日付ヘルパーの存在とJSON-LD構文をbuildに追加。Node標準の回帰テストを追加し、現在の本体コードを読み込み/import | `npm test`, `npm run build` |
| `README.md`, `AGENTS.md`, `docs/SITE_POLICY.md`, `docs/CLINIC_FACTS.md` | Cloudflare運用・テスト手順・データ更新を現状へ整理。全11 HTMLにrobots metaのnoindex指定なし、robots.txtはAllow:/と記録。今回限定の除外事項を明記 | コードと照合。CNAME/配信基盤変更なし |

## 再修正不要・保護対象

- 受付時間に応じた「翌日」「次の診療日」と担当医の案内、日本時間の毎分更新・日付更新は開始時に実装済み。その方針を維持し、データ異常時の扱いだけ補強しました。夜23:59から0:00では「明日」から当日朝の受付案内へ切り替わります。
- 水曜12:00、通常日/土曜12:30、午後14:00〜17:30の受付、日祝・臨時休診、第4土曜と通常土曜の担当表は既存方針を保持。
- 胃カメラの鎮静なしで運転可能・すぐ帰宅・休憩不要の比較と説明、鎮静ありの運転禁止・休息等の注意は開始時と完全一致。別節・FAQ・JSON-LDへの迂回注意書きも追加なし。
- `service.html`のワクチン節全体、`JS/news-data.js`全文、`CNAME`全文は開始時と完全一致。ワクチンの2種類/3種類、接種期間、予約方法を維持。商品名・比較・回数・料金等の詳細は追加なし。
- 胃がん検診の既存鎮静管理料、検査日基準、無料対象者も課金対象という説明を保持。通常診察の予約不要、胃カメラは定期通院を含め事前電話予約、現金のみの案内を保持。
- 対象外のFAQ JSON-LDも変更前との比較で一致。担当を分けた読み取り専用の独立確認でも、禁止箇所・料金・予約に新たな矛盾なし。

## 一次資料と確認日

すべて2026-09-15確認。公式ページの公開/更新日が読み取れないものは作業日で代用していません。

- [佐世保市 がん検診を受けましょう](https://www.city.sasebo.lg.jp/hokenhukusi/kenkou/gan/gankensin.html): 令和8年度の料金・対象条件。ページ内に令和7年度の生年月日例が混在しているため転載せず、既存無料対象者説明を保持。ページ更新日は未確認。
- [個人情報保護委員会 Q2-8](https://www.ppc.go.jp/all_faq_index/faq3-qb2-8/)、[医療・介護ガイダンス](https://www.ppc.go.jp/personalinfo/legal/iryoukaigo_guidance/): 医療提供に必要な範囲、利用目的掲示等による黙示同意と、明確な同意が必要な場合。ガイダンスは2026年4月改正版を参照。院内の実運用を確認済みとはしていません。
- [厚生労働省 健康日本21アクション支援システム 脂質異常症](https://kennet.mhlw.go.jp/information/information/metabolic/m-05-004): HDL低値を含む説明。掲載更新日2022-12-23。
- [Schema.org availableService](https://schema.org/availableService)、[MedicalTherapy](https://schema.org/MedicalTherapy): 許容されるサービス型と意味。ページ改訂日は未確認。検索順位・リッチリザルトを保証しません。
- [内閣府 国民の祝日について](https://www8.cao.go.jp/chosei/shukujitsu/gaiyou.html): 2027年までのデータ、2028年分は2027年2月掲載予定の案内を確認。今回の更新スクリプト試験は通信モックで行い、実CSVの再取得は未実施。既存の1955〜2027年・1067日をそのまま使用。
- [Node.js v22.17.0 test runner](https://nodejs.org/download/release/v22.17.0/docs/api/test.html): Node22系標準のnode:testを使用。新規依存なし。

## 検証環境と結果

- Node.js v26.8.2、npm 11.19.1。最低要件はNode22以上のまま。Node22実行環境での再試験は未実施。
- Chrome 153.0.8010.36 / Playwright CLIの既存キャッシュを使用。GA宛通信を遮断。ローカル開発サーバー `http://127.0.0.1:4173` のみを試験。
- 初回の検証はNodeテスト69件、ブラウザー26シナリオ（UI10・日付9・障害7）。後続の料金表・サイト全体のUI検証は末尾に追記しています。

## 未確認・医院側の確認・別案

- 個人情報共有について、院内の利用目的掲示、説明、明確な同意が必要なケースの実運用は医院で確認が必要です。一律の書面同意ルールは新設していません。
- 市の令和9年度（2027年4月以降）の検診基本料金・対象条件は未確認。今回の合算表では保証しません。鎮静管理料は既存どおり2027年1月以降に適用する説明を保持。
- 祝日CSVの実取得、更新workflowの実行、Cloudflare上の最新編集の404応答/配信ヘッダー、プレビューの非登録、検索エンジンの実登録は未検証。既存日を削除する公式訂正は自動更新を停止し、出典確認後に手動対応する設計です。
- 実機iPhone/Android、Safari/Firefox、支援技術の実機読み上げ、Lighthouse、axe、Core Web Vitalsは未実施。エミュレーション幅での確認を実機検証や性能スコアと扱いません。
- 小田医師のふりがな・経歴・資格・写真は未確認のため追加なし。
- 全HTMLのテンプレート化、静的サイトジェネレーター、React/Next.js移行、全面的CSS分割は実施せず、今後必要になれば別作業として検討する範囲です。

## 今回分の取り消し

開始時ファイルのアーカイブは `/private/tmp/clinic-review-aab6067-baseline.tar` に保存しています。最終差分の逆適用用patchは `/private/tmp/clinic-review-2026-09-15.patch` に保存します。どちらも一時領域なので、後日使う場合は別途保管が必要です。

取り消す時点でブランチ・未コミット差分を確認し、まず `git apply --reverse --check /private/tmp/clinic-review-2026-09-15.patch` を実行してください。開始時以降の別作業と重複していないことを確認したうえで、同じpatchを `git apply --reverse` します。check失敗や後続編集がある場合は、変更箇所を個別に照合し、他者の作業を残してください。reset、強制上書き、履歴書き換え、未確認差分の一括破棄は不要です。この作業では取り消し自体は行っていません。

## 再実行手順

通常の回帰検証は、リポジトリで次を実行します。

```sh
npm test
npm run build
git diff --check
```

ブラウザーの再試験コードは `tests/browser/ui.js`（画面/操作）、`tests/browser/dates.js`（時計/掲載期間）、`tests/browser/failures.js`（障害/実用導線）です。稼働中のローカル開発サーバーと既存Playwright CLIが必要です。今回使用したCLIは `/Users/ko/.npm/_npx/31e32ef8478fbf80/node_modules/@playwright/cli/playwright-cli.js`。キャッシュの場所は環境によって変わります。新しいnpm依存としては追加していません。

`npm run dev` を別ターミナルで起動し、CLIで専用Chromeセッションを開いたうえで、各コードを順番に実行します（共有ブラウザーに対して同時実行しない）。例：

```sh
node /Users/ko/.npm/_npx/31e32ef8478fbf80/node_modules/@playwright/cli/playwright-cli.js -s=clinic-review open about:blank --browser=chrome --headed
node /Users/ko/.npm/_npx/31e32ef8478fbf80/node_modules/@playwright/cli/playwright-cli.js -s=clinic-review --raw run-code --filename=/Users/ko/Developer/clinic-homepage/tests/browser/ui.js > /private/tmp/clinic-review-ui-results.json
node /Users/ko/.npm/_npx/31e32ef8478fbf80/node_modules/@playwright/cli/playwright-cli.js -s=clinic-review --raw run-code --filename=/Users/ko/Developer/clinic-homepage/tests/browser/dates.js > /private/tmp/clinic-review-dates-results.json
node /Users/ko/.npm/_npx/31e32ef8478fbf80/node_modules/@playwright/cli/playwright-cli.js -s=clinic-review --raw run-code --filename=/Users/ko/Developer/clinic-homepage/tests/browser/failures.js > /private/tmp/clinic-review-failures-results.json
node tests/check-browser-results.mjs /private/tmp/clinic-review-ui-results.json /private/tmp/clinic-review-dates-results.json /private/tmp/clinic-review-failures-results.json
```

CLIの成功終了だけでは個々のシナリオ合格を意味しません。必ず最後の結果チェックを通します。テストデータは通信差し替え/VM/一時出力だけに使い、公開のお知らせデータには混ぜていません。ブラウザーの時計を変えますが、Macの時計は変更しません。

## 最終検証結果

- `npm test`: **69成功、0失敗、0スキップ**。本体JSをVMで読み込み、更新スクリプト本体をimport。時刻・通信・DOMの必要部分だけをテスト用に差し替えています。
- `npm run build`: **成功**（必須25ファイル・11 HTML・JS構文・ローカル参照・JSON-LD構文）。開始時のbuildも成功しており、未解決のbuildエラーはありません。
- `node tests/check-browser-results.mjs ...`: **26シナリオ成功、0失敗、0スキップ、予期しないJavaScript例外0**。375/390/768/1440px × 主要6ページで横はみ出しなし。FAQ/目次、Tab/Enter/Space/Escape、通常/動きを減らす設定のメニュー、駐車場拡大、発信を防いだ電話リンク、戻る、受付時間/アクセス移動を確認。
- 東京/UTC/LA/ホノルルで同じ掲載日・掲載可否。開いたまま深夜更新、記事DOM/フォーカス/同日スクロール維持、popupの表示直前期限切れ・表示中期限切れ・閉じた内容の再表示防止を確認。
- 日本時間2026-09-14 23:59:59から0:00をまたぐと、上部の「明日」案内が **「開院前／9:00から受付開始」** へ、カレンダー案内も当日へ更新されることを実ブラウザーで確認。休診日の場合は当日の休診と次の受付を案内します。
- 取得失敗テストでは意図したリクエストのabortを使用。これを通常アクセスの障害とは数えていません。JavaScript無効、共通JS/祝日/ニュース/日付ヘルパー取得失敗を確認。
- 深いURL `/not-a-page`、`/missing/deep/page`、`/missing/deep/` はローカルで **HTTP 404**。各404の追加リソース失敗0、ホームと診療案内へ復帰可能。
- 禁止箇所を再比較し、原文保持を確認。最終のroot再比較8項目と、読み取り専用担当による独立確認10項目が成功しました。新規のワクチン詳細・鎮静なしの注意書き追加はありません。
- 実装途中に見つかったメニュー/拡大表示のfocus失敗はCSSで修正。FAQのhashchangeと即時開閉が競合する問題は、リンククリック時に履歴を更新して同期的に開く処理へ修正。いずれも再試験で合格しました。
- 検証コード側の初回の失敗（描画完了前の判定、hidden属性ではなく存在しないaria-hidden属性の期待、スマホで非表示のPC用リンクやhrefの誤指定、0時の実文言との期待値違い）も、実装を調べて検証を訂正し再実行しました。テスト削除・無条件スキップはしていません。
- `git diff --check`: 成功。開始時SHAとブランチは変更なし。全変更を未コミットのまま保持。

画面確認には `/private/tmp/clinic-review-faq-390.png`、`/private/tmp/clinic-review-fees-390.png`、`/private/tmp/clinic-review-calendar-1440.png` を使用し、実画像も確認しました。結果JSONとスクリーンショットは一時領域のため、長期保存は保証しません。


## 料金表の表示修正（追加確認）

Chromeのfile表示で、料金表の補足と次の案内文が重なる報告を受けて修正しました。`.visit-note` の負の上余白（PCで−13.6px）が原因で、文字の描画範囲が約7px重なっていました。胃がん検診の節に限って上余白を24pxにし、料金表の見出しと開始時期の文字サイズを分け、金額を読みやすいゴシック体に変更して右揃えに統一しました。本文・金額・開始時期は変更していません。

今回の変更ファイルは `gastroscopy.html`、`CSS/style.css`、`tests/browser/fee-layout.js`、この検証記録です。以前の検証は横はみ出しが中心で、この段落間の重なりを検出できていませんでした。追加の試験はRangeで実際の文字の描画範囲を測り、段落間隔と各セルのはみ出しを検証します。

再実行は専用Chromeセッションとローカルサーバーを起動後、既存CLIの `run-code --filename=/Users/ko/Developer/clinic-homepage/tests/browser/fee-layout.js` を使用します。fileとHTTPそれぞれで375/390/768/1200/1440pxを確認し、通常のスクロール演出完了後も画像確認します。結果JSONは `tests/check-browser-results.mjs` で確認できます。

追加修正の検証結果：料金表のブラウザー10ケース成功（0失敗・0スキップ・JS例外0）。修正後の文字間の縦の空きは約30pxで、重なり・セルの欠け・横はみ出しなし。390pxでは無料対象者の見出しを均等に折り返し、一文字だけの行を解消しました。`npm test` 69件と `npm run build`、`git diff --check` も再実行して成功。今回もcommit・push・公開は行っていません。

## トップのリンク整理・サイト全体のUI調整（追加依頼）

既存の未コミット変更を引き継ぐ継続作業です。今回の追加変更ファイルは `index.html`、`CSS/style.css`、`gastroscopy.html`、`service.html`、`facility-standards.html`、`tests/content-consistency.test.mjs`、`tests/browser/site-layout.js`、本書です。

- トップの「初めての方へ」「受付時間・カレンダー」「アクセス・駐車場」「胃カメラのご案内」を、左の本文の下に2列×2行で集約。右側の重複リンクを削除。スマホでも同じ順序で電話カードの前に配置し、同じ大きさの押しやすいリンクにしました。電話番号・受付時間・受診方法の内容は保持しています。
- トップの写真がヒーローの下へ14%はみ出し、発熱案内の本文を覆う問題を実画面で確認。ヒーローの描画範囲を限定し、写真が案内文に重ならないようにしました。
- スマホの診療案内・生活習慣病ページの項目一覧は、文字が細切れになる2列から1列に変更。胃カメラの6項目目次はPCで3列×2行、スマホで2列×3行に統一しました。
- 診療案内の検診料金表は、スマホで70歳以上の列が画面外に隠れていました。検診名と検査内容を同じ見出しセルにまとめ、年齢別料金と合わせて3列に変更。見出しセルと各金額を対応させ、検査内容・年齢・全6金額は保持しています。
- 補足文の負の上余白は、余白の大きい結論文の直後だけに限定。リスト直後などの段落には正の余白を確保しました。前回の胃がん検診節の補足修正も保持しています。
- 地名・書面掲示・カレンダーの途中で見出しが切れる箇所は、意味のまとまりで折り返す指定に変更。トップの案内文の文字サイズを調整し、受付時間表ではスマホの時刻を別行にして「今日」が縦に割れない幅を確保しました。

追加ブラウザーコード `tests/browser/site-layout.js` は、実際に配信された11 HTMLと本体CSS/JSを読み込み、320/375/768/1024/1440pxで文字ノードのRange、表のセル、リンクの配置を検証します。読み上げ専用のクリップされたラベルは可視文字から除外。横はみ出しだけでなく、本文同士の重なり、料金表の各列の欠け、受付時間表の幅も検査します。4リンクのキーボード順序・実際の移動先と、通常のアニメーションが終わった後の表示も確認します。

再実行は既存Playwright CLIで `run-code --filename=/Users/ko/Developer/clinic-homepage/tests/browser/site-layout.js` を使い、結果を `tests/check-browser-results.mjs` に渡します。新しいテストフレームワークやnpm依存は追加していません。

変更禁止箇所の追加比較は9項目で一致：`JS/news-data.js`、`CNAME`、ワクチン節全体、胃カメラの鎮静剤比較と注意事項、経口・経鼻の説明、胃カメラ・初診案内・診療案内の運転/休息/帰宅の説明行、検診表3行の金額・内容・年齢。FAQ・JSON-LDへの迂回注意書きやワクチン詳細の追加はありません。

最終検証結果：

- `npm test`: 69件成功、0失敗・0スキップ。
- `npm run build`: 必須25ファイル・11 HTMLの検証成功。`git diff --check` も成功。
- ブラウザー: **102シナリオ成功**（今回のサイト全体検証66、既存UI10、日付9、障害7、料金表10）。0失敗・0スキップ・予期しないJS例外0。結果は `/private/tmp/clinic-site-*-results.json` に保存。
- 全11ページを5画面幅で計測し、可視文字の重なり・横はみ出し、料金セルの欠け、受付時間表のはみ出しなし。通常の演出完了後のトップ、スマホの項目一覧・見出し・料金表・駐車場・医師紹介・支払い案内を実画像でも確認しました。320px・375pxの最後の改行調整も画面を再確認しています。
- 料金表はfile/HTTPそれぞれの5画面幅でも成功。リンク移動・FAQ・メニュー・深夜更新・データ取得失敗はローカルHTTPで確認。
- 従来どおり実機Safari/Firefox・支援技術・Cloudflare配信の検証は未実施。今回のUI調整で新たな医院運用の確認事項はありません。既存の保留事項は上記に記載。
- 追加依頼の開始時の未コミット差分は保持。ブランチ・HEADは変わらず、commit・push・PR・公開なし。確認先は `http://127.0.0.1:4173/`。

画面例: `/private/tmp/clinic-site-final-home-1440.png`、`/private/tmp/clinic-site-final-home-375.png`、`/private/tmp/clinic-site-final-checkup-table-375.png`、`/private/tmp/clinic-site-final-hours-320.png`。一時領域のため長期保存は保証しません。

## トップ4リンクの操作感と電話カードの整理（追加依頼）

今回の追加変更は `index.html`、`CSS/style.css`、`JS/script.js`、本書です。既存の未コミット変更を保持して続けています。

- 電話カード内の「詳しく見る」を削除し、検査・点滴の受付終了間際の案内をカードの横幅全体で表示。受付時間表の詳しい注意事項は維持しました。
- 左の4リンクは同じ色・書体・サイズ・影に統一。「初めての方へ」だけの強調はありません。淡いグラデーションと丸い矢印を共通で使い、ホバー時は背景色が左から広がり、カードが少し浮き、矢印が進むようにしました。
- キーボードのフォーカスを明示し、タッチでも押下が分かる反応を追加。標準のスクロール・リンク移動は妨げず、指を離す・スクロール・操作中断・ページ移動で押下状態を解除します。「動きを減らす」設定では移動・拡縮・アニメーションを止め、色による反応を残します。

最終確認：`npm test` 69件成功、`npm run build` 成功、`git diff --check` 成功。実際の本体HTML/CSS/JSをローカルChromeで読み込み、320/375/768/1024/1440pxの配置、4リンクの見た目の一致、ホバー・マウス押下・キーボードフォーカス・動きを減らす設定・タッチ押下を確認し、9シナリオ成功（失敗・スキップ・JS例外0）。タッチのキャンセル、スクロール、実際のアクセスページへの移動と「戻る」でも押下表示が残らないことを確認しました。初回のタッチ確認で標準の `:active` が反応しない状態を再現し、Pointer Eventsによる本体修正後に成功しています。

今回の開始時のHTMLと比較し、本文の差は削除した「詳しく見る」のラベルだけであることを確認。診療方針・診療内容・鎮静剤の説明・ワクチンの記載は変更していません。医院運用の追加確認事項はありません。実機Safari/Firefoxは未確認です。commit・push・PR・公開は行っていません。

追加の確認コードは `/private/tmp/clinic-hero-effects-check.js`、結果は `/private/tmp/clinic-hero-effects-results.json`。画像 `/private/tmp/clinic-hero-effects-rest-1440.png`、`/private/tmp/clinic-hero-effects-rest-375.png`、`/private/tmp/clinic-hero-effects-hover-1440.png` を目視確認しました。一時領域のため長期保存は保証しません。

## 胃がん検診の受診案内の表現調整（追加依頼）

ユーザーの依頼により、症状・治療歴等から「対象外」と一律に言い切る説明を、次の相談案内へ変更しました。

> 症状やこれまでの検査・治療の状況をうかがい、受診方法をご案内します。電話予約時・診察時にお気軽にご相談ください。

変更箇所は `service.html` の検診説明と、`gastroscopy.html` の検診本文・FAQ・FAQ JSON-LDの計4箇所です。年齢・住民票・職場での受診機会・年度内1回の案内と、市の公式案内へのリンクは保持。2026-09-15に[佐世保市の一次資料](https://www.city.sasebo.lg.jp/hokenhukusi/kenkou/gan/gankensin.html)を再確認し、市の制度変更や症状がある場合の助成適用を確定する文言は追加していません。

`tests/content-consistency.test.mjs` は、以前の断定文の語句を必須にしていた検査を改め、実際のHTML本文・FAQとJSON-LDの相談案内が一致することと、基本条件が残ることを検証します。今回の変更ファイルは以上の2 HTML・既存テスト・本書の4ファイルです。

`npm test` 69件成功、`npm run build` 成功、`git diff --check` 成功。追加依頼の開始時と2 HTML全体を比較し、指定の4箇所以外は完全一致を確認しました。鎮静剤の使用有無に関する説明、運転・帰宅・休息の案内、ワクチン情報、料金・期間の変更はありません。今回は文言変更のため、ブラウザーでのレイアウト再検証は実施していません。未コミット差分を保持し、commit・push・PR・公開は行っていません。

## 公開承認と最終検証

上記のローカル確認後、ユーザーから「OKこれでマージして公開よろしく」と明示的な承認を受けました。それ以前の「未コミット・未公開」の記述は承認前の作業記録です。

公開対象は `codex/calendar-next-day-guidance` のカレンダー改善と、本書に記録したレビュー対応・UI調整・胃がん検診の表現変更です。公開前に `npm test` 69件、`npm run build`、`git diff --check` を再実行し、すべて成功。originの最新mainを取得し、保存していた最終差分との一致も確認しました。通常のGitマージとmainへのpushで、既存のCloudflare Pages連携から公開します。
